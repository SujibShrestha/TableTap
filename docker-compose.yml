services:
  backend:
    build: 
      context: ./backend
      dockerfile: Dockerfile

    container_name: tabletap-backend

    ports:
      - "3000:3000"

    env_file:
      - ./backend/.env

    restart: unless-stopped
